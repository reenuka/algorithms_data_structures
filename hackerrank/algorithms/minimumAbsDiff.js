// Given an array of integers, find and print the minimum absolute difference between any two elements in the array.
// I: array of integers
// O: number representing largest absolute difference between any two elements in the array
// C: number of integers in array > 2 and < 10,000, values are > -10,000,000,000 and < 10,000,000,000
// E: none?

function minimumAbsoluteDifference (arr) {
    // declare variable to track minimum difference
    // iterate over array
        // 
    // return minimum difference
}